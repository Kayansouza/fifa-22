const loadProducts = (produtos, iddivparent) ={
    const parentDiv = document.querySelector (productld)
    produtos.array.forEach(element => {
        
    });

    
    
        
   
    
                parentDiv.insertAdjacentHTMl('beforeend, html')
    }

    loadproducts(produtos,#product-div)